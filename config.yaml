listen_port: 2222

# Log level: Controls logging verbosity
#   debug  - All messages including forwarding details
#   info   - Normal operational messages (default)
#   warn   - Warnings and errors only
#   error  - Only error messages
#   silent - No logs (except fatal errors)
log_level: info

# Circuit breaker: Automatically disable failing proxies
#   true  - Enable circuit breaker (default) - proxies with 3+ errors in 1 minute are temporarily disabled
#   false - Disable circuit breaker - continue using all proxies regardless of errors
circuit_breaker_enabled: false

host_key: 

# single SOCKS5 proxy (deprecated - use 'socks_list' instead)
# socks5_address: 192.168.10.201:7000

# Multiple SOCKS5 proxy configurations with round-robin and circuit breaker
socks_list:
  - address: 192.168.10.201:7000
    username:
    password:
  - address: 127.0.0.1:7000
    username:
    password:


# single user config (use 'users' list instead)
# username: user0
# password: pass0

# Multiple user configurations
users:
  - user: user1
    password: pass1
  - user: user2
    password: pass2